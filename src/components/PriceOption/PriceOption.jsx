import PropTypes from 'prop-types'

const PriceOption = ({option}) => {
    const {name, features} = option;
    return (
        <div>
            
        </div>
    );
};

PriceOption.propTypes = {
    option: PropTypes.object
}

export default PriceOption;